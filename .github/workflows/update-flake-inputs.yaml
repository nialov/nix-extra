# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/update-flake-inputs.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  update-flake-inputs:
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - id: app-token
      uses: actions/create-github-app-token@v2
      with:
        app-id: ${{ secrets.APP_ID }}
        private-key: ${{ secrets.APP_PRIVATE_KEY }}
    - uses: actions/checkout@v5
      with:
        token: ${{ steps.app-token.outputs.token }}
    - continue-on-error: true
      uses: cachix/cachix-action@v14
      with:
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        name: nialov
    - uses: DeterminateSystems/nix-installer-action@v9
    - uses: mic92/update-flake-inputs@main
      with:
        auto-merge: false
        github-token: ${{ steps.app-token.outputs.token }}
    timeout-minutes: 60
'on':
  schedule:
  - cron: 0 2 * * 0
  workflow_dispatch: {}
